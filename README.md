# Testing_repo

Throwaway test target for the AI Code Review Assistant project. Not a real application —
a small Python + TypeScript codebase used to exercise AST parsing, semantic retrieval, and
the review pipeline against real PRs and diffs.

Do not add production logic here. Do not treat findings against this repo as representative
of code quality; some patterns here are intentionally left rough to give the reviewer
something to find.

Also used to exercise the PR Validation Sandbox (Phase 6) against real install+test cycles.
